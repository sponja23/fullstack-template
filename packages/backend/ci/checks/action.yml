name: Checks for @repo/backend
description: Run static checks and integration tests.
runs:
  using: composite
  steps:
    - name: Static checks
      shell: bash
      working-directory: packages/backend
      run: pnpm exec vp check --no-error-on-unmatched-pattern .
    - name: Test
      if: ${{ !cancelled() }}
      shell: bash
      working-directory: packages/backend
      run: pnpm exec vp test run

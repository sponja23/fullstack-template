name: Checks for @repo/telemetry
description: Run static checks.
runs:
  using: composite
  steps:
    - name: Static checks
      shell: bash
      working-directory: packages/telemetry
      run: pnpm exec vp check --no-error-on-unmatched-pattern .

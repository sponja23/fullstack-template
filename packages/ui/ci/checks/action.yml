name: Checks for @repo/ui
description: Run static checks.
runs:
  using: composite
  steps:
    - name: Static checks
      shell: bash
      working-directory: packages/ui
      run: pnpm exec vp check --no-error-on-unmatched-pattern .
